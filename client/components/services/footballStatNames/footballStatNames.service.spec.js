'use strict';

describe('Service: footballStatNames', function () {

  // load the service's module
  beforeEach(module('abroadathletesApp'));
  beforeEach(module('socketMock'));

  // instantiate service
  var FootballStatNames;
  beforeEach(inject(function (_FootballStatNames_) {
    FootballStatNames = _FootballStatNames_;
  }));

  it('should do something', function () {
  });

});
