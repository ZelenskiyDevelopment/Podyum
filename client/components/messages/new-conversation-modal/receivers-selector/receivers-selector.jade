.receivers-selector(layout='column')
  md-content(layout='column').md-padding.autocomplete
    md-contact-chips(
    ng-model="contacts"
    md-contacts="querySearch($query)"
    md-contact-name="name"
    md-contact-image="image"
    md-require-match
    filter-selected="filterSelected"
    placeholder="To")
