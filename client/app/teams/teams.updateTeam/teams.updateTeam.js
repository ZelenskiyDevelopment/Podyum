'use strict';

angular.module('abroadathletesApp')
    .config(function($stateProvider){
       $stateProvider.state('teams.updateTeam',{
            url:'',
            templateUrl:'',
            controller:''
       });
    });